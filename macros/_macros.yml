version: 2

macros:
  - name: drop_all_user_schemas
    description: '{{ doc("drop_all_user_schemas") }}'
    arguments:
      - name: database
        type: string
  - name: generate_schema_name_example
    description: "{{ doc('generate_schema_name_example') }}"
    arguments:
      - name: custom_schema_name
        type: string
      - name: node
        type: dict